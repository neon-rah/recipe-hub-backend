spring.application.name=backend

spring.datasource.url=jdbc:postgresql://localhost:5432/kalyartdb
spring.datasource.username=neon
spring.datasource.password=ne03on01
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# JWT Secret Key
jwt.secret=MA_CLE_SECRETE_SUPER_SECURISEE

# JWT Expiration Time (15 minutes pour l'Access Token)
jwt.access-token.expiration=900000

# JWT Refresh Token Expiration (7 jours)
jwt.refresh-token.expiration=604800000
